import { ProfileContent } from './components/ProfileContent';

export const metadata = {
	title: 'Панель управления — EventSync',
};

export default function ProfilePage() {
	return <ProfileContent />;
}
